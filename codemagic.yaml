workflows:
  android-release:
    name: Android Release APK

    environment:
      node: 20

    scripts:
      - npm ci
      - cd android
      - ./gradlew assembleRelease

    artifacts:
      - android/app/build/outputs/apk/release/*.apk